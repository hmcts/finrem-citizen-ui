{% from "govuk/components/phase-banner/macro.njk" import govukPhaseBanner %}
{% from "govuk/components/footer/macro.njk" import govukFooter %}

{% extends "govuk/template.njk" %}

{% set serviceName = "Dividing your money and property" %}

{% block pageTitle %}
  {% if errors and errors.length %}Error: {% endif %}{{ pageTitle }} - {{ serviceName }} - GOV.UK
{% endblock %}

{% block head %}
  {% include "webpack/css.njk" %}
  {% if dynatrace and dynatrace.enabled %}
    <script type="text/javascript" src="{{ dynatrace.url }}" crossorigin="anonymous"></script>
  {% endif %}
{% endblock %}

{% block header %}
  {% include "macros/cookie-banner.njk" %}
  {{ govukHeader({
    homepageUrl: "/",
    containerClasses: "govuk-width-container"
  }) }}
  {{ govukServiceNavigation({
    serviceName: serviceName,
    serviceUrl: "/",
    navigation: [
      {
        href: "/logout",
        text: "Sign out"
      }
    ]
  }) }}
{% endblock %}

{% block govukFooter %}
  {{ govukFooter({
    meta: {
      items: [
        {
          href: appRoutes.cookies,
          text: "Cookies"
        }
      ]
    }
  }) }}
{% endblock %}

{% block beforeContent %}
  {% set feedbackLinkHtml %}This is a new service – your <a class="govuk-link" href="{{ feedbackSurveyUrl }}" target="_blank" rel="noopener noreferrer" aria-label="provide feedback on this service (opens in a new tab)">feedback</a> will help us to improve it.{% endset %}
  {{ govukPhaseBanner({
    tag: {
      text: "Beta"
    },
    html: feedbackLinkHtml
  }) }}
{% endblock %}

{% block bodyEnd %}
  {# Run JavaScript at end of the <body>, to avoid blocking the initial render. #}
  {% include "webpack/js.njk" %}
{% endblock %}
