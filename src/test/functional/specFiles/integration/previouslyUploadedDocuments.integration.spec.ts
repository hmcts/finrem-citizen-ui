/**
 * PREVIOUSLY UPLOADED DOCUMENTS - INTEGRATION TESTS
 *
 * MIGRATION NOTE: These tests have been moved to the mock suite
 * (src/test/functional/specFiles/mock/previouslyUploadedDocuments.mock.spec.ts)
 * due to session loss issues in integration environments.
 *
 * The mock suite uses deterministic session injection via test-support routes,
 * providing stable, fast coverage of previously uploaded documents functionality.
 *
 * Integration coverage for real CCD workflows can be added here if/when
 * CCD session reliability improves across preview deployments.
 */
