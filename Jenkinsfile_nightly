#!groovy

properties([
  pipelineTriggers([cron('45 10 * * 1-5')]),
  parameters([
    string(name: 'URL_TO_TEST', defaultValue: 'https://finrem-citizen-ui.aat.platform.hmcts.net',
      description: 'The URL you want to run these tests against'),
  ])
])

@Library("Infrastructure")

def type = "nodejs"
def product = "finrem"
def component = "citizen-ui"

def secrets = [
  'finrem-${env}': [
    secret("idam-secret" ,"OAUTH2_CLIENT_FINREM"),
    secret("finrem-idam-client-secret", "FINREM_IDAM_CLIENT_SECRET"),
  ]
]

static LinkedHashMap<String, Object> secret(String secretName, String envVar) {
  [ $class: 'AzureKeyVaultSecret',
    secretType: 'Secret',
    name: secretName,
    version: '',
    envVariable: envVar
  ]
}

def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

withNightlyPipeline(type, product, component) {
  env.TEST_URL = params.URL_TO_TEST
  loadVaultSecrets(secrets)

  // TODO: Enable and implement when these tests are planned and written
  //  For reference see: nfdiv-frontend application
  //enableCrossBrowserTest()
  //enablePerformanceTest()
  //enableMutationTest()

  // 1. Install playwright browsers on the agent
  afterSuccess('build') {
    yarnBuilder.yarn('playwright install chromium')
  }

  // 2. Point the "Full Functional" stage to Playwright script
  before('fullFunctionalTest') {
    env.FULL_FUNCTIONAL_TESTS_COMMAND = "test:full-functional"
  }

  enableFullFunctionalTest(200)

  // 3. Capture Allure and Artifacts
  afterAlways('fullFunctionalTest') {
    allure([
      includeProperties: false,
      jdk: '',
      properties: [],
      reportBuildPolicy: 'ALWAYS',
      results: [[path: 'allure-results']]
    ])
    // Archive the HTML files as backup artifacts
    steps.archiveArtifacts allowEmptyArchive: true, artifacts: 'functional-output/functional/reports/**/*'
  }

  // 4. Ensure A11y also runs nightly
  afterSuccess('fullFunctionalTest') {
    yarnBuilder.yarn('test:a11y')
  }

  enableSlackNotifications('#finrem-dev')
}