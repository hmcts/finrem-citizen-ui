#!groovy

properties([
  pipelineTriggers([cron('00 21 * * 1-5')]),
  parameters([
    string(name: 'URL_TO_TEST', defaultValue: 'https://finrem-citizen-ui.aat.platform.hmcts.net',
      description: 'The URL you want to run these tests against'),
  ])
])

@Library("Infrastructure")

def type = "nodejs"
def product = "finrem"
def component = "citizen-ui"

def secrets = [
  'finrem-${env}': [
    secret("finrem-citizen-s2s-client-secret", "SERVICE_AUTH_SECRET"),
    secret("finrem-citizen-ui-idam-client-secret", "FINREM_CITIZEN_UI_IDAM_CLIENT_SECRET"),
  ]
]

static LinkedHashMap<String, Object> secret(String secretName, String envVar) {
  [ $class: 'AzureKeyVaultSecret',
    secretType: 'Secret',
    name: secretName,
    version: '',
    envVariable: envVar
  ]
}

def yarnBuilder = new uk.gov.hmcts.contino.YarnBuilder(this)

withNightlyPipeline(type, product, component) {
  env.TEST_URL = params.URL_TO_TEST
  loadVaultSecrets(secrets)

  afterSuccess('build') {
    yarnBuilder.yarn('build')
  }

  // Install Playwright ONCE before all functional/a11y test stages
  before('fullFunctionalTest') {
    echo "Preparing environment for tests..."
    sh 'rm -rf functional-output a11y-output allure-results'
    env.FULL_FUNCTIONAL_TESTS_COMMAND = "npx playwright install chromium --with-deps && TEST_RESULTS_DIR=functional-output yarn test:full-functional"
  }

  enableFullFunctionalTest(200)

  afterSuccess('fullFunctionalTest') {
    echo "Functional tests passed. Running Accessibility Tests..."
    sh "yarn test:a11y:report"
  }

  afterAlways('fullFunctionalTest') {
    junit testResults: 'functional-output/*.xml, a11y-output/*.xml', allowEmptyResults: true
    
    archiveArtifacts artifacts: 'functional-output/**, a11y-output/**', allowEmptyArchive: true
    
    fingerprint 'functional-output/playwright-functional-report/index.html, a11y-output/playwright-accessibility-report/index.html'
    
    allure([
      includeProperties: false,
      jdk: '',
      properties: [],
      reportBuildPolicy: 'ALWAYS',
      results: [
        [path: 'functional-output/allure-results'],
        [path: 'a11y-output/allure-results']
      ]
    ])
  }

  enableSlackNotifications('#finrem-dev')
}